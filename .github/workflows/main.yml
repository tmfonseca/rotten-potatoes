name: CI-CD

on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Docker Build and Push
        users: docker/build-push-action@2.7.0
        with:
          context: ./src
          file: ./src/Dockerfile
          push: true
          tags: 
            tmfonseca/rotten-potatoes:latest
            tmfonseca/rotten-potatoes:${{ github.run_number }}

